"use strict";
function multiplicarNumero(num1, num2) {
    return num1 * num2;
}
const valor = multiplicarNumero(15, 35);
console.log(valor);
//------------------------------//
const nomes = (nome) => `Olá ${nome}`;
const resposta = nomes("Eduarda");
console.log(resposta);
